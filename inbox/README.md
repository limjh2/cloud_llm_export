# Cloud LLM Export Inbox

Every item here except this file is pending private intake. Preserve each
provider delivery intact until its provider and delivery date are established.
Real inbox contents are local-only and must never enter template history.

No work happens here. The inbox is a drop point, not a workspace: move a
delivery to `staging/` to examine and describe it, then on to its immutable home
under `exports/<provider>/YYYY-MM-DD/`. Sweep toward empty — anything left here
should be deliberate pending intake.

This folder is untracked apart from this file and `.gitignore`, so material
waiting here is not backed up by git. Move it into the tracked `staging/` early
rather than letting it sit.
